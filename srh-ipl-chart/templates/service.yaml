apiVersion: v1
kind: Service
metadata:
  name: {{ include "srh-ipl-chart.fullname" . }}
  labels:
    {{- include "srh-ipl-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "srh-ipl-chart.selectorLabels" . | nindent 4 }}
